# Secrets & local config
# (real secrets live in .env; commit .env.example instead)
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
env/
.python-version

# Tooling / test caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Project build artifacts
# (data/vector_index/ is rebuilt with `python src/ingest.py`)
# (data/services.json IS committed — it's a graded deliverable)
data/vector_index/
*.faiss

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Node (only if a JS chat UI is added later)
node_modules/

# Working notes / planning docs — kept locally, not part of the public repo.
# CLAUDE.md is the AI-assisted-dev build log; docs/ is planning docs, the
# eval log, the pitch deck, and the hackathon organiser's own slide PDFs.
# None of it is needed to run the product. Delete a line below if you do
# want that content public.
CLAUDE.md
docs/

# Superseded interface — the graded/demo product is the FastAPI + web/
# front-end (src/api.py). src/app.py was the earlier Streamlit chat UI and
# is no longer part of the product; kept on disk for reference only.
src/app.py
